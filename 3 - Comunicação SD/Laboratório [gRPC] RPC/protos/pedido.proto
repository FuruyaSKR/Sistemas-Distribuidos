syntax = "proto3";

package pedido;

service GerenciadorPedidos {
  rpc RegistrarPedido (Pedido) returns (Confirmacao);
  rpc ObterProximoPedido (Solicitacao) returns (Pedido);
}

message Pedido {
  string descricao = 1;
}

message Confirmacao {
  string status = 1;
}

message Solicitacao {}
